{% extends "FrontendBundle::base.html.twig" %}

{% block titre %}Electro El Atoui - Connexion{% endblock %}

{% block location %}
<ul class="breadcrumb">
  <li><a href="{{ path('_backend_homepage') }}"><i class="fa fa-home"></i></a></li>
  <li><span>Connexion</span></li>
</ul>
{% endblock %}


{% block body %}
    {% if error %}
        <div class="alert alert-danger">
            {{ error.messageKey|trans(error.messageData, 'security') }}
            <button type="button" class="close" data-dismiss="alert">×</button>
        </div>
    {% endif %}

    <div class="row">
        <div class="col-sm-6">
            <div class="well">
                <div class="heading">
                    <i class="fa fa-file-text-o"></i>
                    <div class="extra-wrap">
                        <h2>Nouveau Client</h2>
                        <strong>Créer un compte</strong>
                    </div>
                </div>
                <p>En créant un compte , vous pourrez commander plus vite,
                   d'être à jour sur le statut de la commande,
                   et de garder trace des commandes que vous avez déjà faites.</p>
                <a href="{{ path('fos_user_registration_register') }}" class="btn btn-primary">Inscription</a>
            </div>
        </div>
        <div class="col-sm-6">
            <div class="well">
                <div class="heading">
                    <i class="fa fa-key"></i>
                    <h2>Déjà client</h2>
                    <strong>Je suis un client</strong>
                </div>
                <form action="{{ path("fos_user_security_check") }}" method="post">
                    <input type="hidden" name="_csrf_token" value="{{ csrf_token }}" />
                    <div class="form-group">
                        <label class="control-label" for="input-email">Adresse mail</label>
                        <input type="text" id="username" name="_username" value="{{ last_username }}" placeholder="Adresse mail" class="form-control">
                    </div>
                    <div class="form-group">
                        <label class="control-label" for="input-password">Mot de passe</label>
                        <input type="password" id="password" name="_password" placeholder="Mot de passe" class="form-control">
                    </div>
                    <div class="form-group">
                        <input type="checkbox" id="remember_me" name="_remember_me" value="on">
                        <label class="control-label" for="remember_me">Se souvenir de moi</label>
                        <br>
                        <a href="{{ path('fos_user_resetting_request') }}">Mot de passe oublié ?</a>
                    </div>
                    <input type="submit" value="Se connecter" class="btn btn-primary">
                </form>
            </div>
        </div>
    </div>
{% endblock %}
